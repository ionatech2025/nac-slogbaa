<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ===================================================================
         Structured logging configuration for SLOGBAA LMS.
         - Dev profile: human-readable console output with colors
         - Prod profile: JSON-structured output for log aggregation
         - All profiles: traceId, spanId, userId propagation via MDC
         =================================================================== -->

    <!-- Dev profile: colored console output for readability -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) - %msg [traceId=%X{traceId:-none} userId=%X{userId:-anon}]%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.nac.slogbaa" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
    </springProfile>

    <!-- Prod profile: structured JSON output -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.JsonEncoder"/>
        </appender>

        <root level="WARN">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
        <logger name="com.nac.slogbaa" level="INFO"/>
    </springProfile>

    <!-- Default (no profile active) -->
    <springProfile name="default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg [traceId=%X{traceId:-none} userId=%X{userId:-anon}]%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
